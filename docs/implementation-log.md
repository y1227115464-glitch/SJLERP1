# 实施记录

## M1 · 工程基础与账号权限

日期：2026-09-07 至 2026-09-08。状态：首批基础功能已可运行，前后端联调及本地验证已完成。M1 中依赖业务档案和报表的剩余项已在执行计划中明确保留。

### 本轮任务分工

| 工作 | 负责方式 | 交付位置 |
| --- | --- | --- |
| 后端、数据迁移、账号权限和后台任务 | 后端子 agent | `apps/api/` |
| 中文后台、登录、店铺与账号页面 | 前端子 agent | `apps/web/` |
| 权限与任务一致性复核 | 独立安全复核子 agent | 复现后由后端修复并补回归 |
| 运行环境、接口联调、实际数据库验收和文档 | 主 agent | `scripts/`、`infra/`、项目文档 |

### 已实现的能力

- 真实 PostgreSQL 数据库及 Alembic 初始迁移；配置从环境读取，提供项目独立启动方式。
- 内部账号登录、HttpOnly 会话、退出、过期、CSRF/Origin 校验及登录失败限流。
- 账号创建、密码重置、角色/店铺授权、停用和会话撤销；管理员防自锁。
- 店铺档案维护和权限内 CSV 导出，支持多个店铺，保护导出中的公式字符。
- 按角色、店铺、本人私有范围控制查询、下载、日志和后台任务。
- 私有附件上传下载、站内通知、操作审计和审批查询数据基础。
- 独立 RQ worker 执行 `workspace_check`，任务状态持久化、队列恢复调度、失败重试和结果去重。
- 中文后台连接真实 API；尚无订单、广告及库存数据时保留明确空状态。

### 验证记录

- 后端在独立 PostgreSQL schema 中运行 17 项测试通过。
- Alembic 升级、结构一致性检查、回退及再次升级通过。
- 实际 PostgreSQL + Redis + 独立 worker 的 8 组集成验收通过，脚本为 `scripts/verify_m1.py`。
- 集成验收使用一次性测试数据库和独立队列，结束后清理，不将测试店铺、用户、附件或通知写入本地主工作区。
- 四项独立复核发现已修复：工作台日志权限、实际请求体大小限制、快速 worker 错误状态被调度覆盖、非 ASCII CSRF 头异常。
- 本机 API 的存活与依赖检查通过；Web HTTP 200，经 Web 代理完成真实登录、会话读取、空工作台查询及退出，退出后账号接口返回 401。
- Python 依赖一致性、开发脚本语法及 Compose 本机监听配置通过检查。
- 前端 `typecheck`、生产构建通过；官方 npm 源依赖审计报告为 0 漏洞。已对业务页面拆包，主包 gzip 约 304 KB，仍有 Vite 的体积提示。
- 本地预览入口为 `http://127.0.0.1:5173/`，管理员登录信息保存在被 Git 忽略且权限为 600 的 `.local/admin-credentials.txt`。本轮未进行浏览器点击、截图及交互验收。

### 范围与限制

- 当前角色为固定的公司管理员、店铺主管、运营、财务和仓库；公司管理员拥有全部店铺范围。独立主体档案和具体业务敏感字段会随 M2/M4 模型继续接入和验证。
- 当前实际后台任务是基础资料与附件可访问性检查。订单、广告和库存的导入解析器尚未实现。
- 审批只有数据与权限查询基础，采购审批流由 M5 实现。
- 经营日期控件仅预留上下文，尚无经营数据时不据此展示虚构报表。
- 本轮使用本地独立 PostgreSQL 18.6 和 Redis 8.2.9 验证；电脑没有 Docker，因此容器启动尚未实测。Compose 当前提供本地数据库/队列设施，不是生产部署包。
- 原一期、二期的完整 ERP 仍按 M2–M8 推进；没有将采购、物流、财务月结移出范围。

### 下一步

继续 M2 的商品、SKU 映射、成本、供应商、仓库及期初数据；实际订单/广告样本在 M3 解析器实施前补齐。

## 2026-09-08 · 发布到本地

- 已重新构建网站，并通过 `scripts/dev.py run --built` 启动构建产物、本地 API 和后台任务；入口保持 `http://127.0.0.1:5173/`。
- 复用已有 PostgreSQL、Redis、账号和文件存储，数据库迁移已确认，无数据重置。
- 已核对网页与 `apps/web/dist/index.html` 一致、静态资源可读取、数据库和队列就绪，并通过网站代理验证登录、工作台和退出。
- 仅监听本机，未设置开机自启；重启方式见 [本地开发与启动](local-development.md)。

## 2026-09-08 · 商品与供应商管理

### 已交付

- 商品管理、供应商管理、采购报价三个页面与真实 API；商品支持图片、规格、中文/英文名称、参考标识、售价、原始来源、筛选及启停。
- 商品 CSV 先预览后确认，原文件与 27 个字段完整保留；错误行阻止整批确认，重复确认与并发确认保护，已有 SKU 不覆盖人工修改。
- 供应商档案、按包装规格拆分的报价、多商品关联、阶梯起订量、单价、独立含税价、贴标费用、运输说明、原文和待核对状态。
- 新增迁移 `cac0e1679f69`，主库已应用；变更前已保存私有本地备份。
- 已重新构建并启动本地版本，入口仍为 `http://127.0.0.1:5173/`。

### 实际资料

- 35 个商品，来自用户 `products_rows.csv`，两个品牌分别为 19 条和 16 条。
- 29 行供货原表归档为 2 家供应商、33 个报价变体；桌卡和 CD 标签重复来源合并保留，空白报价不覆盖有效价格。
- 7 条报价尚无对应商品，14 条报价标记待核对；缺失联系人、起订量、税况和价格保留为空或未注明。
- 原 CSV 售价单位待用户确认，本轮以 `unknown` 导入，35 个商品参考售价留空并保留原始价格；采购报价独立以原文人民币金额保存，不受这一待确认项影响。
- CSV 中重复 FNSKU、规格数量矛盾和清理空格等情况均保留说明，不自动合并商品或改写原始资料。
- 原文件、供货整理 JSON、导入回执及核对脚本保存在被 Git 忽略的 `.local/imports/`，真实供货价格不放入源代码仓库。

### 验证结果

- PostgreSQL 全量 28 项测试通过，包含权限、商品/供应商/报价维护、金额边界、并发导入和整批回滚。
- Alembic 升级、模型一致性检查、回退和再次升级通过。
- 独立复核 30 项检查通过；发现的极端 CSV 金额返回 500、SQLite 保存点导致部分提交两项问题已修复并独立复测通过。
- 对 35 条商品逐行比较原始字段、行号、图片及标识；对 33 条报价逐项比较关联、阶梯数量、金额精度、含税价、贴标费和原文，均一致。
- 再次运行真实初始化脚本：商品新增 0、供应商新增 0、报价新增 0；原记录 ID 和更新时间保持不变。
- 中文搜索、品牌筛选、未匹配及待核对筛选、页面及全部前端静态资源、API/数据库/Redis 就绪检查通过。
- 前端类型检查与生产构建通过，无新增依赖；仍有既有 Vite 包体积提示。本轮未进行浏览器点击或截图验收。

### 后续边界

商品和供应商是公司共享档案，不按店铺日期筛选；参考 ASIN/FNSKU 尚未建立店铺映射。报价不自动生成标准成本、采购单或利润，CSV stock/monthly_sales 不作为实时经营数据。后续继续按 M2–M8 完成剩余业务模块。


## 2026-09-08：采购、物流与库存联动

已部署采购记录、发货进度与库存管理，复用局域网入口 http://192.168.31.23:5173。支持采购草稿/提交/未分配余量取消、两段物流及供应商直发 FBA、运单/Shipment ID、物流事件、分批接收、仓库档案、期初/调整、占用/释放、库存余额及不可编辑流水。账号与店铺范围生效，仓库角色不可见采购金额。

验证：PostgreSQL 35 项通过；SQLite 33 项通过，2 个行锁并发用例在 PostgreSQL 验证；前端类型检查和构建通过；独立评审修复两项 P2 后复核通过。浏览器完成采购 100、本批发 60 收 40、国内仓发 25、FBA 收 20，结果国内 15 / FBA 20 / 在途 25，数量守恒。测试全在隔离 schema 或 SQLite，未写入正式业务库。

本轮局域网正式实例迁移前后实际数据一致：商品/供应商/报价均 0，店铺 2、账号 1。早期商品导入验证记录不能作为当前运行实例已有资料的依据，开始采购前仍需实际建档或导入。新仓库、采购、货件及库存为空，没有复制验收数据。升级前已备份本机 PostgreSQL 与前端，迁移到 9fa3ed5d01d2；主页、登录及新查询接口实测通过，数据库/队列健康正常。

使用说明见 [采购、发货与库存](supply-chain.md)。规格、评审、验收与上线记录归档于 [.free-flow/_archive/specs/202609/2026-09-08-supply-chain](../.free-flow/_archive/specs/202609/2026-09-08-supply-chain/)。FBA 仓为人工接收账面量，不代表实时可售库存；审批、质检退货、装箱费用分摊、报表核对和自动补货继续保留在 M5/M6，不标记整体完成。

## 2026-09-08 · 亚马逊销售与广告导入

已在既有局域网入口部署「数据导入中心」「销售记录」「广告数据」。销售支持订单 TXT/TSV，按店铺、渠道、订单号和 SKU 跨文件去重，较新版本更新、旧版跳过、同时间冲突阻止整批确认；广告支持所给中文商品推广 XLSX，保留统计区间和 7 天归因窗口，同期替换、非相同重叠区间拒绝。上传预览与确认分开，支持批次来源、店铺权限、分页查询、日期/关键词筛选及币种汇总。

原始样本在隔离 PostgreSQL schema 验证：销售 354 条、广告 41 条。销售文件换名重传跳过 354 条，再传重叠 100 条全部跳过；金额和数量与独立读取的源数据核对一致。兼容该 XLSX 错误的 A1 工作表范围。用户文件、业务金额和凭据未加入版本库，正式店铺未导入这两份样本。

独立评审修复广告同值确认的版本保护、损坏共享字符串 XLSX 的 500 响应，并优化区间比较；复核通过。PostgreSQL 全套 48 项通过；SQLite 45 项通过、3 个 PostgreSQL 行锁用例跳过；前端类型检查、构建、依赖一致性及迁移升级/回退/再次升级检查通过。隔离浏览器实测两类上传、重复合并、确认、明细、来源、搜索与日期筛选，使用合成数据。

16:14 升级前备份数据库及旧前端到 `.local/backups/amazon-imports-20260908-161407/`；正式迁移至 `de1aefc77089`，局域网首页、新页面资源、5 个报表查询和既有采购/库存/商品接口均返回 200，数据库与 Redis 健康正常。此次部署前后的实际数据一致：商品 34、供应商/报价 0、店铺 2、账号 1，采购/物流/库存表均 0；新报表批次和事实表也为空。与之前采购上线时的商品 0 条记录属于不同时间点，未覆盖用户此后新增资料。

使用说明见 [亚马逊销售与广告导入](amazon-report-imports.md)，规格与验收见 [.free-flow/_archive/specs/202609/2026-09-08-amazon-imports](../.free-flow/_archive/specs/202609/2026-09-08-amazon-imports/)。M3/M4 剩余报告适配、商品映射、撤回、利润、工作台指标和销售扣库存继续保留未完成状态。

## 2026-09-08 · 广告日报按天覆盖

按用户新要求，日报以店铺、日期、SKU、广告活动名称和广告组名称为身份，新导入覆盖原指标与属性；ASIN、币种、国家等不参与日报去重。兼容「日期」列及同日起止日期，同文件重复保留最后源行。增加日期和广告组预览列，日报与旧版多日汇总分别查询/汇总。

独立评审修复旧日报同批次迁移并列时随机 ID 选行的问题，按最新版本和最后源行保留；原导入批次留存。PostgreSQL 54 项全部通过，SQLite 50 通过 / 4 个行锁用例由 PostgreSQL 覆盖，前端构建及隔离迁移升降检查通过。隔离浏览器实测旧日花费 3 更新为 8，次日新增 4，日报合计 12；旧多日汇总 30 单独显示。

16:45 备份至 `.local/backups/ad-daily-20260908-164539/`，正式迁移 de1aefc77089 → 2c638fa481b9。局域网资源、11 个新旧查询、登录和数据库/Redis 健康检查通过；原有商品 34、销售 354、批次 1、店铺 2、用户 1 均保留，广告为空，未写入验收记录。详细验证 `.local/ad-daily-live-verification.json`。

新版原始附件因 macOS 剪贴板目录读取限制未取得，文件选择器尝试也未上传成功；上述为合成日报及明确业务规则验收，不声称已实测该附件。规格、评审及验收见 [.free-flow/_archive/specs/202609/2026-09-08-ad-daily-dedup](../.free-flow/_archive/specs/202609/2026-09-08-ad-daily-dedup/)。

## 2026-09-08 · 销售与广告 SKU 搜索候选

销售记录和广告数据增加共享候选搜索框：聚焦空框显示当前店铺、日期和状态/粒度范围内的去重 SKU，输入只更新匹配候选，选中后精确搜索明细与汇总。支持方向键选择、回车或按钮确认关键词、清除搜索；候选最多显示 50 个并提示继续输入。无迁移及新依赖。

独立评审通过，PostgreSQL 全量 57 项通过，新增 SQLite 定向 3 项通过，前端类型检查和构建通过。隔离浏览器实测两页面空框/匹配/选择/清除，销售键盘选择与直接回车、广告无 SKU 匹配时搜索活动名称；明细和汇总一致，输入过程不触发业务搜索。

17:24 备份至 `.local/backups/report-search-20260908-172444/` 后更新局域网入口 http://192.168.31.23:5173。正式候选、精确 SKU 明细/摘要、页面资源、健康与既有业务查询通过；商品 34、销售 354、广告日报 273、批次 2、店铺 2、账号 1 均保留。主库版本保持 2c638fa481b9，未写入测试业务数据。

使用方法见 [SKU 候选搜索](amazon-report-imports.md#sku-候选搜索)，规格、独立评审、验收和发布记录归档于 [.free-flow/_archive/specs/202609/2026-09-08-report-search-suggestions](../.free-flow/_archive/specs/202609/2026-09-08-report-search-suggestions/)。

## 2026-09-08 · 报表日期快捷入口

销售和广告页面添加最近7天、最近3天、今天、最近30天、本周、上周、本月、上月、今年以来九个快捷按钮，点击后立即同步筛选明细和汇总。按浏览器本地日历计算，最近 N 天含今天、周一开始、当前周期截至今天；手动日期和全部日期取消快捷高亮，其他搜索与筛选保留。

4 组日期测试、前端类型检查/构建、3 项相关报表 API 回归通过；独立评审在上海、洛杉矶、奥克兰时区复测各 4 组通过。浏览器实测全部快捷值、日期与 SKU 组合、清空、手动日期、无效区间提示及键盘操作。17:44 备份后更新既有局域网入口；无数据库迁移，销售 354、广告日报 273 等业务数据保留，健康、资源和相关接口核对通过。

验收及发布记录：[报表日期快捷入口](../.free-flow/_archive/specs/202609/2026-09-08-report-date-shortcuts/report-date-shortcuts-verify.md)。

## 2026-09-08 · 一体式报表范围日历

按用户参考图，将销售和广告日期选择改为单个起止输入框、双月日历和左侧九项快捷列表，增加「含当天」开关（仅作用于最近3/7/30天）。支持日历手选、直接输入、快捷高亮、键盘确认和清除，保留既有 SKU 与其他筛选。

独立评审定位并修复部分手选后点快捷项被关闭草稿覆盖的问题，最终复核通过；浏览器回归确认最终范围和请求稳定，并验证两页面汇总、含当天切换、输入/取消/清除和组合筛选。日期测试 5 组、类型检查与最终构建通过，无新依赖或迁移。

18:13 备份后更新局域网版本，页面资源与接口核对通过；销售 354、广告日报 273 等原有数据保留。详细记录：[范围日历验收与发布](../.free-flow/_archive/specs/202609/2026-09-08-report-range-calendar/report-range-calendar-verify.md)。

## 2026-09-08 · 周度销售、补货与发货设计调整

根据用户每周五分析销售并采购、供应商随后一周供货、下周四确认可交量并建 Amazon 货件、下周五/周六统一出货的节奏，新增 [周度工作流设计](weekly-operations-workflow.md)。主要调整为本周出货/新补货双区、商品销售周表、按可售时间计算补货、供应商确认与周四齐货、计划/实发/接收分列，以及旧采购跨周保留和供给去重。

同步修订产品规划、技术边界、开发顺序和当前操作说明入口；新增 W1–W5 待实现任务，保留 M1–M8 原建设范围。库存运输天数、实际路线、安全量和包装规则保持待配置，未把样例参数写成经营默认值。设计规格见 [weekly-operations-spec.md](../.free-flow/_archive/specs/202609/2026-09-08-weekly-operations/weekly-operations-spec.md)，状态为 draft，设计编制步骤已完成。

校验：文档内部链接、代码块闭合、9 月两轮周度日期、200 件原始补货/216 件整箱示例，以及 100 下单/60 齐货/50 实发/30 接收的数量守恒通过；`git diff --check` 通过。本次仅为设计与文档交付，没有修改应用代码、执行数据库迁移或部署，也没有写入业务数据、联系供应商或在亚马逊后台建单；未运行应用测试，不宣称新流程已通过功能验收。

## 2026-09-09 · 灵活作业与工作台待办设计修订

根据用户补充，将上一版固定周度编排调整为独立业务操作与可选提醒模板。采购、分析、发货可随时进入，不以周度批次、固定星期、供应商跟进或前一项待办完成作为前置；库存数量、权限与过账正确性校验继续保留。

新增五类模板：采购创建后下一次周一09:00下单、预计发货前2个自然日确认生产、预计发货当天跟进、每天15:00下载经营数据、每周五分析销售与库存。明确预计发货与到货不同、未指定时分采用全天事项、改期跟随/手动覆盖、提前完成、分批/取消、周期单次与未来修改、重复调度及离线补生成。工作台默认我的待办，支持完成、改期、取消与来源跳转，任务状态与通知已读/业务事实分开。

同步 [工作流文档](weekly-operations-workflow.md)、产品规划、技术方案、设计规格、README与当前操作说明；开发计划增加优先交付W0，待办不等待FBA快照/完整补货模型。原W1–W5能力保留为独立增强，尚未实现。

设计校验通过：本次9份Markdown的链接和代码块、周一09:00前/正点/之后的日期边界，以及09-18发货对应09-16生产跟进、改09-20后对应09-18的示例；`git diff --check`通过。仅改文档，无应用代码、数据库迁移或部署，未创建ERP/Codex实际提醒；未运行应用测试，不宣称提醒已可运行。


## W0 · 灵活工作台待办与单据提醒

日期：2026-09-09。状态：已实现、独立评审和验收通过，已更新原局域网实例。

- 工作台改为人的待办，支持手动新建、完成、取消、恢复、改期、负责人、来源、记录和权限内筛选；提醒不限制采购或发货。
- 五类可配置规则：每日15:00经营数据、周五销售库存分析、创建采购后的周一09:00下单、发货前两天生产确认、发货当天跟进。全天与定时语义分开；支持暂停、单次覆盖、离线追补及去重。
- 采购/货件新增预计发货日，与预计到货分开；已下单补录、生产确认记录、分批货件提醒覆盖和余量恢复均保留业务事实口径。
- 独立scheduler、持久化事件和周期游标、事务锁、失败退避、汇总通知全部来源关联；完成待办不回写数量，权限变化后再次校验。
- 独立评审发现并关闭七项并发/日期/通知/深链问题。最终PostgreSQL 71项全通过，SQLite 64通过/7个PG专用跳过，前端5项测试、类型与构建通过。迁移往返和结构一致性通过，隔离浏览器验证已完成。
- 已部署迁移48b7a65dc109；备份`.local/backups/w0-20260909T091258Z/`。核心业务行数与停写备份相同，API、数据库、Redis和scheduler正常，未自动启用用户规则。

使用说明：[工作台待办与提醒](task-workbench.md)。W1–W5 的FBA快照、补货计算和计划内部分实发等仍独立推进。
