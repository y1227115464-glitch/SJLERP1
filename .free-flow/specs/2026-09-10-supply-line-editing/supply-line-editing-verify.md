---
doc_type: verify
slug: supply-line-editing
status: pass
verified_at: 2026-09-10
---

# 验收记录

- 先行测试3项因缺少商品编辑接口404失败，实现后通过。
- 后端 SQLite 全量：80 passed、9 skipped（PostgreSQL专属项）。
- 本机 PostgreSQL 独立临时测试库：test_supply_amendments、test_supply、test_packing 共19 passed；含修改与收货竞争、同一请求并发重试、不同修改版本竞争。测试库已清理，不修改业务数据库。
- 前端 npm --prefix apps/web run build：通过，已有主包大于500kB提示。
- 前端7项现有测试：Codex自带Node执行 --experimental-strip-types --test apps/web/tests/*.test.mjs，7 passed。
- git diff --check：通过。

## 契约证据

| 项目 | 验证 |
|---|---|
| 已提交采购修改数量、追加商品 | 保留行ID和单价；金额重算；可正常从原货件接收 |
| 下限与输入错误 | 已取消＋分配下限、重复SKU、漏掉原行、缺新增价格、修改原价均拒绝；失败回滚 |
| 发货SKU替换、数量修改 | 采购可分配余量跟随更新，整箱校验；支持待发及在途 |
| 收货历史保护 | 已收SKU不能移除或数量减少到已收以下；多次部分接收会更新版本；已收齐拒绝修改 |
| 采购已收齐追加 | 数量增加后恢复部分到货，已收与库存不改 |
| 库存联动 | 待发占用差额、已发实物差额、可用量不足回滚、重复提交不重复过账 |
| 权限与审计 | 运营403、未授权店铺404、审计不含原价、长说明完整保留且摘要不超500 |

## 浏览器交互

本机Chrome无头实例加载本次构建产物，通过完全模拟的API响应验证，不连接实际业务库。

1. 已下单详情点击「编辑商品及数量」，改20为24、追加SKU-B并填数量单价；确认提交原行不携带单价、新行携带商品ID、版本正确；弹窗关闭、列表和详情刷新。通过。
2. 在途详情进入「修改产品及数量（不建议操作）」；确认警示显示，数量改为12；模拟409后确认输入12保留；重试保存成功、弹窗关闭并刷新。通过，未捕获页面JS异常。
3. 初次浏览器启动缺少Playwright自带浏览器，改用本机Chrome；测试定位受带图标按钮及嵌套对话框影响，读取实际按钮状态后修正测试定位，无业务代码改动。

未更改店铺/供应商/仓库或历史接收流水。尚未提交Git或部署应用。
