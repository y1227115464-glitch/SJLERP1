# 独立评审记录

2026-09-10，独立 reviewer 对新文件与 diff 只读检查五个维度；最终复查通过。

1. 草稿 confirm 未重新验证商品关系：已修复；新增回归测试先验证失败，再确认修复后通过。
2. 货件多选只支持当前页：已按筛选路径缓存已选完整记录并启用 preserveSelectedRowKeys；切店/筛选清空。浏览器跨页选择两张并成功合并。

其他检查未发现可确认的权限、库存总量、采购分配、锁序或幂等问题。项目未配置额外整洁工具。

## 资源红线自查

| 本次适用项 | 结果与证据 |
| --- | --- |
| 商品范围查询按需投影、索引、去重 | product_scope.py:21–38，EXISTS + ProductStore 复合主键、QuoteProduct 复合主键/SupplierQuote供应商索引；不展开报价明细 |
| 关系维护有范围与并发锁 | product_scope.py:64–78，校验店铺权限，锁商品串行维护 |
| 合并批量、有界且锁序一致 | supply/merge.py:15–42，最多100货件，批量锁采购单再按ID锁货件 |
| 合并不重复动库存 | supply/merge.py:69–93，只合计明细并标注来源，不调用change_stock；发货与取消沿用原扣减流程 |
| 金额与异常 | 本次无实际资金流水，跟进不使用缓存；异常交统一API回滚，无吞异常 |
| 历史列表有界 | supply/finance.py:38–43，按采购ID及时间索引取最近50条 |
| 前端切范围不显示旧选项 | PurchasePages.tsx:105–108、124、130，上下文变化重建选择器并清空行；useResource按path绑定数据 |
| 跨页选择与状态清理 | ShipmentPages.tsx:18–24、32，缓存选择，path变化清空，后端校验每张货件版本 |
| 加载与分页 | ProductStores.tsx 表格分页；候选仅打开时请求limit30；新增组件随原页面懒加载，无新增重依赖 |
