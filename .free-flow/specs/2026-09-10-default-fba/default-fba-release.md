# 发布说明

需一起更新 API 和 Web；前端依赖本次新增的缺省仓库解析与 warehouse_kind 查询参数。

无数据库结构迁移、配置变更或历史数据改写。首次缺省仓库业务写入可能在同一事务中新建 FBA-DEFAULT。

按现有本地部署流程发布前备份数据库，更新前后端并刷新页面。回归：新建发货默认供应商/FBA；顶部期初显示 FBA；库存默认 FBA；余额行调整锁定原归属。回退时前后端一并回退，保留已生成仓库及正常单据流水。

本次未执行部署。
