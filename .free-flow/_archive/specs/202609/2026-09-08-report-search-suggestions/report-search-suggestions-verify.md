# 验收记录

2026-09-08，独立评审通过后执行回归和界面验收。

- 新增候选 API 用例先运行得到 3 个 404 失败，实现后 SQLite 定向 3 项通过。涵盖去重、包含/字面匹配、数量和输入上限、精确 SKU、汇总、店铺/日期/状态/广告粒度及权限。
- PostgreSQL 独立 schema 全量 57 项全部通过（21.62 秒），包含导入去重与库存并发；正式业务数据未参与测试。
- 前端 TypeScript 与 Vite 构建通过，产物暂存 `.local/report-search-web-next/`。保留既有 Vite 大包提示，无新增依赖。
- `git diff --check` 通过。

## 浏览器操作

隔离 SQLite/API 8001/Web 5174，销售和广告各 4 条合成记录，共 3 个不同 SKU。

| 操作 | 实际结果 |
|---|---|
| 两页面聚焦空框 | 显示 SKU-A、SKU-AB、SKU-B，无重复项 |
| 输入 SKU-A | 候选仅 A、AB，明细仍 4 条，未发起明细/汇总搜索 |
| 鼠标选择 SKU-A | 销售精确 2 条、净额 39.9600；广告精确 2 条、花费 6.0000，不包含 AB |
| 销售直接回车确认 SKU-A | 普通关键词命中 A 和 AB，共 3 条、净额 59.9400 |
| 销售方向键选择后回车 | 实际选中 SKU-AB，精确 1 条；日志确认列表和摘要仅 sku=SKU-AB，没有 q 覆盖 |
| 清除按钮 | 恢复 4 条并重新显示空框候选 |
| 广告输入「活动」 | 提示无匹配 SKU，明细保持原查询的 2 条 |
| 广告搜索按钮确认「活动」 | 按活动名称命中 4 条、花费 12.0000 |

浏览器截图检查候选宽度、输入与搜索按钮排列正常。使用日志 `.local/logs/report-search-ui-api.log` 核对输入只请求 suggestions，选择后列表与 summary 使用相同精确条件。
