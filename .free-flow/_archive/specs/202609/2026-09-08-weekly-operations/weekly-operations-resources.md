# W0 资源红线自查

| 检查 | 证据与结论 |
|---|---|
| 列表与历史上限 | `tasks/routes.py:listing/detail` 复用服务端分页，详情历史最多100；采购生产记录最多50；人员建议最多50；规则应用最多20。 |
| 调度有界 | `tasks/scheduler.py:periodic/tick/deliver` 每事务90个日历日、每轮30个周期/30个事件、最多200项通知；`next_run_at` 避免反复扫描未来日。 |
| 查询与批量 | 实例使用`insert_ignore`批量写入；通知与全部关联任务集中写入；单采购聚合一次加载子货件及关联待办，行关系用selectin。扫描以来源、游标和状态索引限制；API聚合不拉全量任务回Python。 |
| 事务与幂等 | 业务事务只追加事件；scheduler采购→货件锁顺序与供应链一致，锁后刷新。实例身份唯一、请求指纹、版本冲突、投递版本；PostgreSQL屏障回归覆盖旧事件与新日期、规则编辑与外键锁。 |
| 异常恢复 | 单事件SAVEPOINT、异常类型日志、attempts/retry_at退避；无fire-and-forget。主循环异常记录日志并重试，SIGTERM可退出。 |
| 权限和敏感信息 | 原会话/CSRF；后端来源/店铺/负责人权限重查；通知为通用文案，TaskNotice按仍可见任务筛选；没有缓存金额和库存。 |
| 前端请求与状态 | `useResource`绑定路径且清理过期请求；分页、搜索防抖。30秒可见页面刷新，focus/interval在卸载时清理；切店清理深链并重挂载工作台和单据页。 |
| 前端资源 | 沿用Ant Design和分页面懒加载，无新依赖、图片或外部资源；主包原有500KB提示保留，任务代码独立共享chunk。 |
| 无外部资源变更 | 无邮件/消息发送、购买、Amazon写入、Codex自动化。真实库只在备份后做增量迁移；测试均在隔离schema。 |
