# W0 工作台待办独立评审

日期：2026-09-09
评审者：独立只读评审代理 `review_w0`
依据：`/Users/jammy/.codex/skills/free-review/SKILL.md`、项目 `.free-flow/attention.md`、本迭代 W0 规格。

## 评审结论：通过

初审发现的七项问题已修复，复核未发现遗留阻塞问题。此结论为代码评审通过；浏览器验收、整体回归及实际部署由实施者继续执行并记录，不用本报告替代。

范围：`apps/api/app/tasks/`、迁移 `48b7a65dc109`、采购和货件的事件/日期接入、通知权限、`TaskShared.tsx`、`TaskRules.tsx`、`WorkspacePage.tsx`、采购/货件页面、`App.tsx` 及 `scripts/dev.py`。本轮复用多个货件分批发货，不评审 W4 的单货件内部分实发扩展。

## 初审问题及修复复核

| 初审级别 | 问题 | 修复证据与复核结果 |
| --- | --- | --- |
| P1 | 两个调度进程处理同一采购单时，旧事件可将待办改回旧日期。初审 PostgreSQL 屏障复现为采购 09-25、待办 09-19、未处理事件 0。 | `apps/api/app/tasks/sources.py:16` 的 `lock_source` 按采购、货件顺序锁定并刷新记录；事件处理及手动应用规则均经过该入口。货件修改复用相同业务锁顺序。并发改期回归通过。 |
| P1 | API 接受极端预计发货日期，偏移运算溢出后事件一直占据队头，阻塞无关周期任务。 | `apps/api/app/supply/schemas.py` 限制预计发货日期为 1901–2199 年；`apps/api/app/tasks/schemas.py:17` 限制待办日期为 1900–2200 年，覆盖偏移余量。`sources.py:127` 使用保存点隔离失败事件，记录错误类别、重试次数和退避时间。非法日期拒绝、失败隔离及恢复回归通过。 |
| P2 | 从待办深链进入甲店单据后切乙店，旧 hash 导致重新打开甲店单据。 | `apps/web/src/App.tsx` 的店铺选择回调先清除单据/店铺 hash 参数，再更新店铺；页面按店铺重新挂载。代码路径复核通过，实际界面回归由实施者记录。 |
| P2 | 汇总通知只关联首条任务，首条被转派或撤权后，其他有效待办的通知一起消失。 | `TaskNotice` 保存汇总通知的全部任务关联；`apps/api/app/routes.py:201` 按至少一条当前可见、仍由收件人负责的任务判断可见性。通知正文不保留业务细节。转派及撤权回归通过。 |
| P2 | 已通知待办转派后，新负责人收不到提醒。 | `apps/api/app/tasks/routes.py` 在负责人变化时递增投递版本，投递仍使用任务行锁和版本去重。原负责人剩余汇总通知与新负责人通知的回归均通过。 |
| P2 | 调度先锁周期游标、编辑先锁规则，插入任务的外键锁导致 PostgreSQL 死锁。 | `apps/api/app/tasks/rules.py` 的规则编辑与应用使用 PostgreSQL `FOR NO KEY UPDATE`，保持版本检查互斥，同时兼容插入任务所需的外键 `KEY SHARE` 锁。规则编辑/调度屏障回归通过。 |
| P2 | 推荐下载及周五分析待办没有导入/分析页面入口。 | `apps/api/app/tasks/scheduler.py:39` 仅从明确的推荐模板身份映射 `imports`/`sales` 来源；普通自定义周期规则不推断页面。前端沿用来源链接。模板来源回归通过。 |

## 五个维度

1. **规范合规：通过。** 沿用 FastAPI、SQLAlchemy、店铺权限、CSRF、审计和请求幂等；迁移为静态定义。独立评审没有修改实现或正式业务数据。
2. **潜在 bug：通过。** 复核日期边界、时区/全天语义、版本冲突、重复创建/投递、改期覆盖、任务转派、撤权、暂停/追补、部分货件完成/取消及失败事件隔离。初审确定问题均已关闭。
3. **架构分层：通过。** 业务事务持久化事件意图，后台进程负责生成和同步待办；完成、取消或调整待办不会回写采购、发货或库存事实。通知已读保持独立语义。提醒状态没有加入业务前置校验。
4. **可维护性：通过。** 日期算法、任务权限、规则、事件同步、调度及展示按职责分开。检查了路由注入参数与跨行辅助函数参数；未发现需要为本轮正确性额外引入通用工作流引擎或请求包装的情形。
5. **资源与性能：通过。** 工作台、规则和关联待办分页；负责人选择有搜索和结果上限；后台事件/周期/通知每轮有处理上限；货件聚合批量查询任务与明细，查询具备对应索引；前端定时刷新及 focus 监听有清理，页面按店铺重挂载。调度在独立进程运行，异常具备日志与持久化诊断信息。

## 独立复核证据

- SQLite：`pytest -q tests/test_task_dates.py tests/test_tasks.py tests/test_task_scheduler.py`，**11 passed**。
- PostgreSQL：`pytest -q tests/test_task_concurrency.py`，**3 passed**。覆盖并发创建/投递幂等、旧事件不能覆盖新日期、规则编辑与周期生成的锁竞争。
- PostgreSQL 验证使用测试 fixture 自动创建和清理的独立 schema；没有访问或修改真实店铺业务记录。
- 现有两条 Starlette/TestClient 弃用提示不由本次功能引入，不影响以上结果。

## 非阻塞测试建议

`apps/api/tests/test_task_concurrency.py:test_postgres_old_source_event_cannot_overwrite_new_date` 可在日期断言后追加该采购所有事件 `processed_at` 非空且 `error_message` 为空的断言。新增保存点会把事件异常转为退避重试；补充断言可以区分真正处理成功与工作线程发生异常但暂未覆盖日期两种情形。当前实现的聚合锁和刷新路径正确，此建议不阻塞评审通过。

项目未配置额外整洁清理工具，按项目约定跳过。除本评审制品外，评审者未修改仓库文件。


实施者收尾补记：已采纳上述测试加强建议，追加全部相关事件成功处理且无错误的断言；最终PostgreSQL全量71项通过。
