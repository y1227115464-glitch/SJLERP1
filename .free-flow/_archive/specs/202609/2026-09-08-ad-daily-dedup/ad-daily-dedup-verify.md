# 验收

2026-09-08，独立评审修复后复核通过。

- PostgreSQL 最终全套 54 passed，20.90 秒；包含两个账号、不同 ASIN 的同日并发确认，及旧日报迁移去重。
- SQLite 全套 50 passed、4 skipped，14.28 秒；跳过四个 PostgreSQL 行锁用例，均在上述 PostgreSQL 执行通过。
- 日报新增测试 6 项：两种日期表头、同文件最后行、同日其他属性覆盖、各身份维度隔离、旧预览、旧版区间分离、并发及存量迁移等场景。
- 前端类型检查及构建通过，产物 `.local/ad-daily-web-next`。仍有既有包体积提示与两个 TestClient 弃用警告。
- 隔离 PostgreSQL 升级、Alembic check、回退 de1aefc77089、再次升级和 check 均通过，测试 schema 清理。
- diff 空白检查通过。

浏览器在隔离 SQLite 验收：原 6 月 1 日花费 3；新文件同键含 5 和最后一行 8，同时含 6 月 2 日花费 4。预览新增 1、更新 1、合并重复 1，显示日期与广告组；确认后日报仅 2 条，花费 12。旧多日区间花费 30 在「历史区间」独立显示，不与 12 合计。页面布局检查通过，未写入正式数据。

边界：本次新附件位于 macOS 剪贴板共享目录，脚本读取返回 Operation not permitted；通过本地文件选择器尝试也未成功上传，请求未到达后端。本次按明确需求兼容「日期」列与同日起止日期，并用合成 XLSX 验证，未声称已读取或实测该原始附件。正式导入前仍可在预览中核对具体文件结果。
