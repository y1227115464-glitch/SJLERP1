# 交付验收

2026-09-08，独立评审修复并复核通过后执行。

- PostgreSQL 隔离 schema 全套：48 passed，14.92 秒，包含销售并发确认、既有目录和库存并发用例。
- SQLite 全套：45 passed、3 skipped，11.65 秒；三个行锁用例在上述 PostgreSQL 测试通过。
- `tsc -b && vite build --outDir ../../.local/reports-web-next` 通过。保留既有主包体积提示；后端仍有两个 TestClient 依赖弃用警告。
- Python 依赖一致性检查通过，`git diff --check` 通过。
- 一次性 PostgreSQL schema：升级 head、Alembic check、回退至 9fa3ed5d01d2、再次升级和 check 均通过；旧采购表保留，新广告版本时间字段匹配。测试 schema 已删除。

## 契约证据

| 规格契约 | 执行证据 | 结果 |
|---|---|---|
| 两份真实格式 | 原始销售 TXT 354 条、广告 XLSX 41 条；修复 A1 范围、浮点尾差和表头空格；源数据独立汇总与 API 核对 | 通过 |
| 销售重复导入 | 实际样本换名再传跳过 354 条，重叠 100 条再传全部跳过，销售总行数保持 354 | 通过 |
| 销售版本和粒度 | 合成测试同键折叠、同 SKU 拆行冲突、更新与旧版跳过、同时间不同内容拒绝、跨店铺独立、跨账号同店铺去重 | 通过 |
| 异常文件和原子确认 | 缺列、非法金额/数量/时区、取消空金额、损坏文件/共享索引、公式、整批冲突与回滚测试 | 通过 |
| 广告区间与统计 | 同期替换、部分重叠拒绝、旧预览和同值版本、按币种加总及加权比率 | 通过 |
| 权限与并发 | 授权撤销、跨店铺详情/摘要/确认、仓库拒绝、预览变化、重复/并发确认测试 | 通过 |
| 页面交互 | 登录、两种上传/预览/确认、必填提示、销售重复合并提示、批次来源、查询/搜索、日期选择器与清除、布局截图检查 | 通过 |
| 正式部署 | 备份、增量迁移、局域网健康和业务数据保留 | 见上线记录 |

原始报告在一次性 PostgreSQL schema 验证，控制金额以原 CSV/openpyxl 独立读取计算，再与 API 汇总逐项比较；完成后删除 schema。真实文件、规范明细及金额未加入版本库。私有本地证据为 `.local/report-sample-verification.json`。

浏览器使用独立 SQLite、API 8001 和预览端口 5174，均为合成数据。销售文件 3 行，其中 1 行重复，确认新增 2、合并重复 1，数量 4、USD 净额 39.9600；广告新增 2，展示 2000、点击 20、花费 6、归因销售额 40。SKU-B 搜索后为 1 行且汇总减半；开始日期选择 2026-09-08 后 6 月月报不显示，清除后恢复，来源批次可打开。未将浏览器合成数据或原始样本写入正式店铺。

此为自动测试及代理操作验收，没有冒充用户实际业务验收。当前无商品映射、报表撤回、日月粒度切换、利润或销售扣库，详见 docs/amazon-report-imports.md。
