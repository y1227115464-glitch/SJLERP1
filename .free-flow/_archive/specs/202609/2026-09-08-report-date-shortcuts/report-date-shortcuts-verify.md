---
doc_type: verify
slug: report-date-shortcuts
status: pass
archive_reason: 已更新局域网且上线核对通过
verified_at: 2026-09-08
mode: fast
authorization: 用户明确要求时间区间添加九个快捷入口，按口述小改动直接实现。
---

# 验收契约

销售和广告页面均提供最近7天、最近3天、今天、最近30天、本周、上周、本月、上月、今年以来。按浏览器本地日历计算，最近 N 天含今天，周一为周起点，当前周/月/年截止今天，上周和上月为完整周期。点击同时填入开始/结束日期并筛选，保留已有搜索、店铺、状态和粒度；手动日期和全部日期取消快捷选中。沿用销售 UTC 下单日期、广告源日期与历史区间完整包含口径。

## 前端红线自查

| 命中项 | 证据 |
|---|---|
| 状态更新和请求 | ReportRecords.tsx:25 使用单一 dateRange 更新起止，查询仍复用 context；无新请求入口、effect 或定时器 |
| 有界渲染和稳定标识 | ReportDateShortcuts.tsx:8 固定九项、preset.value 为 key；原明细继续服务端分页 |
| 资源与生命周期 | report-date-ranges.ts:14 点击时计算当前日期；无额外依赖，无监听器，仍随 ReportRecords 懒加载 |

## 已执行

- 先运行日期测试得到缺少实现文件的失败；实现后 4 组测试通过，覆盖九个区间、跨年、闰年二月、周日/周一及本地日期。
- TypeScript 和 Vite 构建通过，暂存 `.local/report-dates-web-next/`；主包大小保持原水平，原 Vite 大包提示仍在。
- 独立只读评审 report_review 通过，无阻断项，未配置整洁清理工具。评审者在 America/Los_Angeles、Asia/Shanghai、Pacific/Auckland 分别运行日期测试，各 4 组通过。
- 后端现有报表范围/候选/权限回归 `pytest -q tests/test_report_suggestions.py`：3 passed；未修改后端，不重复全套库存并发测试。
- `git diff --check` 通过。

## 浏览器验收

独立 SQLite/API 8001/Web 5174，销售和广告各 7 条合成数据。以 2026-09-08 为本地今天。

- 销售逐个点击九个快捷入口，日期值全部符合契约，始终只有点击项被选中。最近7天为 09-02 至 09-08，明细 4 条、净额 79.9200；今年以来为 01-01 至 09-08，明细 6 条、净额 119.8800。
- 通过原生日期控件键盘调整开始日期，快捷高亮取消。手动构造开始晚于结束时，原有明细/汇总错误提示均正常；全部日期清空起止和高亮并恢复 7 条、139.8600。
- 广告上月为 08-01 至 08-31，2 条、花费 6.0000。保留 SKU-A 搜索并切最近3天后为 09-06 至 09-08，2 条、花费 6.0000；键盘回车触发今天后为 1 条、3.0000。
- 通过请求日志确认日期一次更新，明细和摘要的 start_date/end_date 保持一致，已有 q 与广告 granularity 保留。截图核对九个按钮及日期说明排列正常。

结论：可交付。无新依赖或数据库迁移，沿用原报表查询与 SKU 搜索；部署后核对另记下方。

## 局域网更新

17:44 备份数据库与旧前端到 `.local/backups/report-dates-20260908-174447/`，备份归档目录读取检查通过，更新 http://192.168.31.23:5173 的构建产物并重启应用。首页与新构建一致，ReportRecords 静态资源、健康、报表和既有业务接口核对通过。

主库仍为 2c638fa481b9，商品 34、销售 354、广告日报 273、批次 2、店铺 2、用户 1 与部署前一致。详情 `.local/report-dates-live-verification.json`。需要回滚时恢复备份的 web-dist 并恢复上个代码版本，再运行 `scripts/local.sh start`；本次无数据库变更，不需要恢复旧库。
